// operand kiri dan kanan operator aritmatika beda tipe harus ditolak
// expect: error type check "Type mismatch di operand kanan '+'"

fn main() -> void {
    let a: u32 = 10;
    let b: i32 = 5;
    let c: u32 = a + b; // u32 + i32
    print_u32!(c);
}
